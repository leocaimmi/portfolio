## What changed

<!-- One or two sentences. What does this do that the previous state did not? -->

## Why

<!-- The reason, not the mechanism. Link an issue if there is one. -->

## Checks

- [ ] `npm run typecheck`, `npm run lint` and `npm test` pass locally
- [ ] Content changes go through `src/content`, not into components
- [ ] New copy exists in both `messages/es.json` and `messages/en.json`
- [ ] Any new animation respects `prefers-reduced-motion`
- [ ] Interactive elements are reachable and operable by keyboard
